info:
  name: Crear usuario
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:2314/users
  body:
    type: json
    data: |-
      {
        "name": "Taiida"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
